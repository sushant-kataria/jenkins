---
- name: Configure Apache
  hosts: all
  become: yes  # Required for installing packages and starting services
  tasks:
    - name: Install Apache
      ansible.builtin.apt:
        name: apache2
        state: present
    - name: Start Apache service
      ansible.builtin.service:
        name: apache2
        state: started
        enabled: yes
