pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project'
                // Add build steps here (e.g., mvn clean install for Java)
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests'
                // Add test steps here (e.g., mvn test for Java)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project'
                // Add deploy steps here (e.g., deploy to a server)
            }
        }
    }
}
